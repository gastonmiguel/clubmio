export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
};

export type Organization = {
    id: string;
    name: string;
    logo: string;
    database_name: string;
    created_at: string;
    updated_at: string;
};
